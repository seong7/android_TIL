<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_view_basic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ex06_reservation"></activity>
        <activity android:name=".basic_practice" />
        <activity android:name=".Calculator" />
        <activity android:name=".SimpleCalculator" />
        <activity android:name=".Ex05_layout" />
        <activity android:name=".Java_layout" />
        <activity android:name=".SimpleCalculator_teacher" />
        <activity android:name=".Ex06_widget" />
        <activity android:name=".sqlite_layout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>